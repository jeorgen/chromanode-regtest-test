chromanode:
  network: livenet # livenet | testnet
  sync:
    maxCachedBlocks: 2 # useful for remote bitcoind

logger:
  level: verbose # verbose, info, warning, error
  filename: master.log

postgresql:
  url: postgres://jorgen:masonit@localhost:17520/jorgen


bitcoind:
  peer:
    host: localhost
    port: 8333
  rpc:
    host: localhost
    port: 8332
    user: jorgen
    pass: masonit
    protocol: http
